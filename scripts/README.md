# Database Seeding Scripts

Этот каталог содержит скрипты для заполнения базы данных тестовыми данными.

## Скрипты

### 1. `seed-users.ts` - Заполнение пользователей (PostgreSQL)

Заполняет таблицу `users` в PostgreSQL тестовыми пользователями.

**Использование:**
```bash
npm run seed:users
```

**Создает:**
- 5 тестовых пользователей с хешированными паролями
- Администратор: `admin@kibtop.com` / `admin123`
- Обычные пользователи: `john@example.com`, `jane@example.com`, `test@example.com`, `seller1@example.com`

---

### 2. `seed-mongodb.ts` - Заполнение MongoDB данных

Заполняет MongoDB коллекции тестовыми данными для всех типов объявлений и связанных данных.

**Использование:**
```bash
# Обычное заполнение (добавляет данные к существующим)
npm run seed:mongodb

# Очистка и заполнение (удаляет существующие данные перед заполнением)
npm run seed:mongodb:clear
```

**Создает данные в 4 коллекциях:**

#### 📝 `advs` (Объявления)
- **13 объявлений** по всем категориям:
  - `realty` (недвижимость) - 2 объявления
  - `avto` (автомобили) - 2 объявления
  - `work` (работа) - 2 объявления
  - `services` (услуги) - 2 объявления
  - `children` (детские товары) - 1 объявление
  - `electronics` (электроника) - 2 объявления
  - `fashion` (мода) - 1 объявление
  - `house_garden` (дом и сад) - 1 объявление
  - `free` (бесплатные) - 1 объявление

**Особенности:**
- Все объявления с координатами городов Северного Кипра (Kyrenia, Nicosia, Famagusta)
- Многоязычные заголовки и описания (en, ru, tr)
- Категорийные поля заполнены (brand, model, year для авто; square, rooms для недвижимости и т.д.)
- Разные статусы (VIP, TOP, обычные)
- Реалистичные цены в EUR

#### ⭐ `fav` (Избранное)
- **5 записей** избранного для каждого тестового пользователя
- Каждый пользователь имеет список ID объявлений в избранном

#### 🔍 `search` (Поисковые запросы)
- **20 популярных поисковых запросов**
- Каждый запрос имеет счетчик использований (`times`)
- Запросы на английском языке

#### 📦 `archive` (Архив)
- **3 архивных объявления**
- Старые объявления с датами 30-60 дней назад
- Разные категории (electronics, house_garden, fashion)

---

## Требования

### Переменные окружения

Убедитесь, что в `.env` файле указаны:

```env
# PostgreSQL (для seed-users.ts)
POSTGRES_URL=postgresql://user:password@host:port/database
# или отдельные переменные:
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=advert
POSTGRES_PASSWORD=advert123
POSTGRES_DB=advert_auth

# MongoDB (для seed-mongodb.ts)
MONGODB_URL=mongodb://localhost:27017
# или для MongoDB Atlas:
# MONGODB_URL=mongodb+srv://username:password@cluster.mongodb.net/
MONGODB_DB_NAME=advert_adverts
```

---

## Порядок выполнения

Рекомендуемый порядок:

1. **Сначала заполните пользователей:**
   ```bash
   npm run seed:users
   ```

2. **Затем заполните MongoDB данные:**
   ```bash
   npm run seed:mongodb:clear  # Очистить и заполнить заново
   # или
   npm run seed:mongodb        # Добавить к существующим
   ```

---

## Структура данных

### User IDs

Скрипты используют хешированные email адреса как user IDs (SHA-256):
- `admin@kibtop.com` → хеш используется как `owner` в объявлениях
- `john@example.com` → хеш используется как `owner` в объявлениях
- и т.д.

### Координаты городов

Все объявления используют реальные координаты городов Северного Кипра:
- **Kyrenia (Girne)**: 35.3392, 33.3192
- **Nicosia (Lefkosa)**: 35.1856, 33.3823
- **Famagusta (Gazimağusa)**: 35.1250, 33.9500

### Валюта

Все объявления используют валюту **EUR** (евро).

---

## Устранение проблем

### Ошибка подключения к PostgreSQL

1. Проверьте, что `POSTGRES_URL` или отдельные переменные правильно указаны в `.env`
2. Убедитесь, что PostgreSQL запущен и доступен
3. Для облачных провайдеров (Neon, AWS RDS) убедитесь, что SSL настроен правильно

### Ошибка подключения к MongoDB

1. Проверьте, что `MONGODB_URL` правильно указан в `.env`
2. Убедитесь, что MongoDB запущен и доступен
3. Для MongoDB Atlas убедитесь, что IP адрес добавлен в whitelist
4. Проверьте правильность учетных данных

### Ошибка "Cannot find module"

Убедитесь, что все зависимости установлены:
```bash
npm install
```

### Дублирование данных

Если вы запускаете скрипты несколько раз без флага `--clear`, данные будут дублироваться. Используйте:
```bash
npm run seed:mongodb:clear
```

---

## Примечания

- Скрипты используют тестовые данные и не должны использоваться в продакшене без модификации
- Изображения используют placeholder URLs (Unsplash). В продакшене замените на реальные URL из S3
- User IDs должны совпадать с реальными пользователями в PostgreSQL для корректной работы избранного
