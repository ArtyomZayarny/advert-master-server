# Исправление ошибки FUNCTION_INVOCATION_FAILED на Vercel

## 🔍 Анализ проблемы

### Что происходило

Ошибка `FUNCTION_INVOCATION_FAILED` возникала при попытке запустить NestJS приложение на Vercel в serverless окружении. Приложение падало во время инициализации, не успев обработать запрос.

### Корневая причина

1. **Отсутствие обработки ошибок в serverless handler**
   - Функция `handler` в `api/index.ts` не обрабатывала исключения
   - При ошибке инициализации приложение падало без ответа
   - Vercel получал необработанное исключение → `FUNCTION_INVOCATION_FAILED`

2. **Блокирующие подключения к базам данных**
   - PostgreSQL, MongoDB и Redis пытались подключиться синхронно при старте
   - Если БД недоступна или переменные окружения не настроены → приложение падало
   - В serverless окружении подключения могут быть медленными или недоступными

3. **Отсутствие таймаутов**
   - Подключения к БД могли висеть бесконечно
   - Инициализация приложения могла превысить лимиты Vercel (10-30 секунд)

4. **Недостаточное логирование**
   - Сложно было понять, где именно происходит ошибка
   - Не было информации о состоянии окружения

## ✅ Внесённые исправления

### 1. Улучшенная обработка ошибок в `api/index.ts`

**Что было:**
```typescript
export default async function handler(req: any, res: any) {
  const app = await createApp();
  return app(req, res);
}
```

**Что стало:**
- ✅ Добавлен try-catch блок для обработки всех ошибок
- ✅ Гарантирован ответ клиенту даже при ошибках
- ✅ Кэширование ошибок инициализации (чтобы не повторять попытки)
- ✅ Детальное логирование для отладки
- ✅ Таймаут на инициализацию приложения (30 секунд)
- ✅ Информативные сообщения об ошибках

**Почему это важно:**
- Serverless функции должны **всегда** возвращать ответ
- Необработанные исключения → `FUNCTION_INVOCATION_FAILED`
- Кэширование ошибок предотвращает повторные попытки на каждый запрос

### 2. Устойчивые подключения к базам данных

**PostgreSQL:**
- ✅ Ошибки подключения логируются, но не блокируют запуск
- ✅ Таймаут 5 секунд на проверку подключения

**MongoDB:**
- ✅ Таймаут 5 секунд на подключение
- ✅ Lazy connection - подключение происходит при первом использовании
- ✅ Не блокирует запуск приложения, если `REQUIRE_MONGODB !== 'true'`

**Redis:**
- ✅ Таймаут 5 секунд на подключение
- ✅ Стратегия переподключения с ограничением попыток
- ✅ Обработка ошибок без падения приложения
- ✅ Не блокирует запуск, если `REQUIRE_REDIS !== 'true'`

**Почему это важно:**
- В serverless окружении БД могут быть недоступны при холодном старте
- Приложение должно запускаться даже если некоторые сервисы недоступны
- Подключения могут происходить "лениво" при первом использовании

### 3. Улучшенная инициализация модулей

**AppModule.onModuleInit:**
- ✅ Таймаут 10 секунд на создание таблиц
- ✅ Ошибки не блокируют запуск приложения
- ✅ Таблицы создаются при первом использовании, если нужно

**Почему это важно:**
- Инициализация БД может быть медленной или недоступной
- Приложение должно отвечать на запросы даже если БД не готова

## 🎓 Концептуальное понимание

### Почему существует эта ошибка?

`FUNCTION_INVOCATION_FAILED` - это защитный механизм Vercel:
- **Защита от зависших функций**: Если функция не отвечает, Vercel её убивает
- **Защита от утечек ресурсов**: Необработанные исключения могут оставлять ресурсы
- **Защита от бесконечных циклов**: Таймауты предотвращают зависания

### Правильная ментальная модель для serverless

1. **Stateless функции**: Каждый вызов независим, нет гарантии состояния между вызовами
2. **Cold start**: Первый запрос медленнее (инициализация), последующие быстрее (кэш)
3. **Таймауты**: Vercel имеет лимиты (10-30 сек для Hobby, до 60 сек для Pro)
4. **Обработка ошибок**: Всегда возвращайте ответ, даже при ошибках
5. **Lazy initialization**: Инициализируйте ресурсы при первом использовании, не при старте

### Как это вписывается в архитектуру

```
Запрос → Vercel → Serverless Function
                    ↓
              Холодный старт? → Инициализация NestJS
                    ↓                    ↓
              Кэширован?              Подключение к БД
                    ↓                    ↓
              Обработка запроса    Таймаут/Ошибка?
                    ↓                    ↓
              Ответ клиенту        Логирование + Продолжение
```

## ⚠️ Признаки проблемы в будущем

### Что искать:

1. **Отсутствие обработки ошибок**
   ```typescript
   // ❌ Плохо
   export default async function handler(req, res) {
     const app = await createApp(); // Может упасть
     return app(req, res);
   }
   
   // ✅ Хорошо
   export default async function handler(req, res) {
     try {
       const app = await createApp();
       return app(req, res);
     } catch (error) {
       res.status(500).json({ error: '...' });
     }
   }
   ```

2. **Синхронные блокирующие операции при старте**
   ```typescript
   // ❌ Плохо - блокирует запуск
   await database.connect();
   await redis.connect();
   await app.init();
   
   // ✅ Хорошо - lazy connection
   // Подключения происходят при первом использовании
   ```

3. **Отсутствие таймаутов**
   ```typescript
   // ❌ Плохо - может висеть бесконечно
   await someAsyncOperation();
   
   // ✅ Хорошо - с таймаутом
   await Promise.race([
     someAsyncOperation(),
     new Promise((_, reject) => 
       setTimeout(() => reject(new Error('Timeout')), 5000)
     ),
   ]);
   ```

4. **Критические зависимости при старте**
   - Если приложение не может работать без БД → используйте `REQUIRE_*` переменные
   - Если может работать частично → делайте подключения опциональными

### Похожие ошибки:

- `FUNCTION_TIMEOUT` - функция превысила лимит времени
- `FUNCTION_MEMORY_LIMIT` - функция превысила лимит памяти
- `MODULE_NOT_FOUND` - отсутствуют зависимости
- `ENOENT` - отсутствуют файлы конфигурации

## 🔄 Альтернативные подходы

### 1. Полностью опциональные подключения

**Плюсы:**
- Приложение всегда запускается
- Гибкость в конфигурации

**Минусы:**
- Ошибки обнаруживаются только при использовании
- Сложнее отлаживать проблемы с БД

**Когда использовать:**
- Когда БД не критична для базовой функциональности
- В development окружении

### 2. Строгие требования к подключениям

**Плюсы:**
- Ошибки обнаруживаются сразу
- Явная конфигурация

**Минусы:**
- Приложение не запустится без БД
- Менее гибко для serverless

**Когда использовать:**
- Когда БД критична для работы
- В production с гарантированным доступом к БД

### 3. Health check endpoints

**Подход:**
- Приложение запускается быстро
- Health check проверяет доступность БД
- Мониторинг отслеживает состояние

**Когда использовать:**
- Для production окружений
- Когда нужен детальный мониторинг

## 📝 Рекомендации

1. **Всегда обрабатывайте ошибки** в serverless функциях
2. **Используйте таймауты** для всех асинхронных операций
3. **Делайте подключения к БД опциональными** в serverless окружении
4. **Логируйте детально** для отладки в production
5. **Тестируйте cold start** локально перед деплоем
6. **Мониторьте логи Vercel** для раннего обнаружения проблем

## 🔗 Полезные ссылки

- [Vercel Error Documentation](https://vercel.com/docs/errors/FUNCTION_INVOCATION_FAILED)
- [Vercel Serverless Functions Best Practices](https://vercel.com/docs/functions/serverless-functions/runtimes/node-js)
- [NestJS Serverless Guide](https://docs.nestjs.com/faq/serverless)
